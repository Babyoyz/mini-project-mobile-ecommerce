import 'package:flutter/material.dart';

class CardShopComponent extends StatelessWidget{

  const CardShopComponent ({super.key});

  @override

  Widget build(BuildContext context) {

    return const Text('Shop');
  }
}